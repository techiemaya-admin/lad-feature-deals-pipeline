/**
 * Chat Step Controller
 * 
 * Re-export for backward compatibility
 * Export directly from the implementation to avoid circular imports
 */
export { useChatStepController } from './ChatStepController/useChatStepController';